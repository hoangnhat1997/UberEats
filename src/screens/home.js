import { SafeAreaView } from "react-native";
import RestaurantItem from "../components/RestaurantItem";

const Home = () => {
  return (
    <SafeAreaView>
      <RestaurantItem />
    </SafeAreaView>
  );
};

export default Home;
